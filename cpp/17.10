include <iostream>
include <vector>
using namespace std;

int main() {
    // создаем вектор, его будем использовать как стек
    vector<char> stack;
   
    stack.push_back('q');    // записываем 'q' в стек 
    stack.push_back('w');    // записываем 'w' в стек   
    stack.push_back('e');    // записываем 'e' в стек
    
    // выводим элементы стека
    cout << "Элементы стека: ";
    for(char i : stack) {
        cout << i << " ";
    }
    cout << endl;
    
    return 0;
}
